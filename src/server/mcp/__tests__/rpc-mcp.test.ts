import { describe, it, expect } from "vitest"

describe("MCP RPC", () => {
  it("supports mcp_list request", async () => {
    // 发 JSON-RPC: { method: "mcp_list" }
    // 期望返回 server 状态数组
    expect(true).toBe(true) // Placeholder - actual test would need full server
  })
})
